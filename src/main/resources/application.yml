spring:
  datasource:
    url: jdbc:mysql://localhost:3306/codeathon
    username: root
    password: Raju@670028
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: false
        jdbc:
          time_zone: UTC
    show-sql: false

logging:
  level:
    root: INFO
    com.company.order: DEBUG

