spring.application.name=demo

# Datasource configuration is specified in application.yml. Main properties file kept minimal for compatibility.
# Please configure the datasource in src/main/resources/application.yml for production (MySQL).
